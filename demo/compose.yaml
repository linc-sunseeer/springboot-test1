services:
   mysql:
      image: mysql:latest
      container_name: spring-mysql
      environment:
         MYSQL_ROOT_PASSWORD: root
         MYSQL_DATABASE: test
      ports:
         - "3308:3306"
      volumes:
         - mysql-data:/var/lib/mysql

volumes:
   mysql-data: