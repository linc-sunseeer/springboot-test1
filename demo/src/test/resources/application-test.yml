spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

  sql:
    init:
      mode: never
  mail:
    username: lin_c@sunseer.co.jp

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  frontend-base-url: http://localhost:5174
  jwt:
    secret: test-jwt-secret-key-for-unit-tests-must-be-at-least-256-bits-long
    expiration-ms: 86400000
  mail:
    enabled: false
