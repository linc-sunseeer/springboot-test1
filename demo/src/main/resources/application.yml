spring:
    datasource:
        url: jdbc:mysql://localhost:3308/test
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update  #自動幫你建表
        show-sql: true    #console 會印 SQL（debug用）